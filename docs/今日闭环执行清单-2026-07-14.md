# 今日闭环执行清单（2026-07-14）

## 范围

- 唯一测试产品：便携恒温杯。
- 真实生成采用 1 条到 3 条的小批量递进，不直接运行 10 条。
- 单价暂不配置，`budget_mode` 保持 `observe`，成本报告标记为待校准。
- 竞品内容只用于结构、节奏、钩子和受众洞察，所有采集结果必须人工审核。

## 执行顺序

- [x] P0 基线：Git 工作区、全量测试、历史真实产物核验。
- [x] P1a TikTok 采集通道1基础版：官方 oEmbed 元数据解析、去重入库、失败隔离。
- [x] P1b 本地运行：`.env.example` 与 Windows 安全启动脚本。
- [x] P2 OpenMontage 参考层：提示词、尺寸、轮询下载、FFmpeg 输出规范。
- [x] P3 真实媒体规格：Seedance 分镜明确 9:16，合成统一到 1080x1920。
- [x] P4 真实 QA：比例、时长、音轨、镜头数、文件和素材映射校验；不合格禁止归档。
- [x] P5a 工作台：Mock/Real 模式、采集方式和产品就绪度。
- [ ] P5b 工作台：供应商失败原因、耗时和运行报告摘要。
- [ ] P6 恒温杯回归：1 条真实生产通过后再运行 3 条，保留报告和录屏。
- [ ] P7 交付：全量测试、运行报告、Claude Code 验收说明、提交并推送。

## TikTok 采集能力边界

| 通道 | 当前状态 | 能力 | 缺口 |
| --- | --- | --- | --- |
| 通道1 官方 oEmbed | 已完成基础版 | 标题、作者、作者链接、封面、标签 | 无互动量、无视频文件 |
| 通道2 授权 API/浏览器辅助 | 待接入 | 互动量、时长、授权范围内的详细元数据 | 需要 TikTok OAuth、Research API 或合规服务凭据 |
| 通道3 人工投递 | 已完成 | 批量链接和人工补充字段入库 | 不自动获取真实元数据 |

不恢复旧仓库的反风控硬爬代码。采集层独立运行，失败不得阻塞在制视频项目。
