# 真实模型最小验收记录（2026-07-20）

## 范围

- 项目：`real-accept-20260720-01`
- 产品：便携恒温杯
- 模式：真实豆包文本模型 + 真实 Seedance 单镜生成
- 预算模式：`enforce`
- 项目预算：`¥35.00`
- 已记账成本：`¥30.13`

## 已验证

- 真实分析、脚本、脚本审核和分镜均成功，脚本与分镜为五段、总时长 30 秒。
- 五个 6 秒竖屏镜头均由真实 Seedance 返回并下载为可解码的 `720x1280` H.264/AAC 视频。
- 每镜抽取 1、3、5 秒画面进行人工检查。
- 镜头 2 至 5 通过产品身份、品牌、动作方向、温标和连续性检查，已选用 Take A。
- 镜头 3 正确展示将奶液倒入恒温杯；镜头 4 正确展示从恒温杯圆形出液口倒入独立奶瓶。
- 工作台能正确显示真实成本、退回镜头、已选镜头和当前 `take_gate` 状态。

## 未放行

- 镜头 1 的显示屏生成约 `90°C`，违反“只允许 `98°F`”的产品硬约束，已标记为 rejected，未选用。
- 剩余预算约 `¥4.87`，低于一次真实单镜重生的估算 `¥5.96`。成本护栏按预期阻止继续消耗，因此没有合成或伪造最终通过结果。
- 代码已加强显示契约：非温度证明镜头强制屏幕熄灭、空白或不可读；仅温度证明镜头允许显示，且必须为 `98°F`。

## 复验条件

将该项目预算提高至少 `¥5.96` 后，仅重生镜头 1。新镜头通过抽帧人工质检后，依次完成 `take_gate`、30 秒合成、最终抽帧目检、归档与反馈入库。

本报告证明真实模型链路已运行到严格人工 Take 闸门，但不把质量不合格或预算不足的项目标记为生产验收通过。
