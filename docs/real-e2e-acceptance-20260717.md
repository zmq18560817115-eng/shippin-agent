# 真实模型端到端验收记录（2026-07-17）

## 结论

本轮已完成真实豆包文本模型与 Seedance 视频模型调用，并验证成本强制拦截、人工脚本闸门、关键帧闸门和逐镜 Take 质检。项目停在 `take_gate`，未虚假放行合成与最终交付。

- 项目：`ref-20260717-040152-244a7c`
- 产品：便携恒温杯
- 模式：真实运行
- 预算模式：`enforce`
- 预算：35.00 元
- 已记录内部估算成本：30.18 元
- 剩余预算：4.82 元
- 状态：`awaiting_human / take_gate`

## 已验证

1. `analysis`、`research`、`strategy`、`script`、`script_breakdown`、`script_review`、`storyboard`、`asset` 均通过真实模型或真实运行路径完成。
2. 真实脚本和分镜为中文结构化产物，保持五段、五镜、每镜 6 秒的 30 秒时间线。
3. 共生成 10 个真实视频 Take（5 镜 x A/B 两版），每个 Take 抽取 1、3、5 秒三帧进行人工检查。
4. Take B 的四个镜头通过产品身份、温标、使用动作和连续性检查；第二镜因奶瓶出现模型虚构文字，被 `no_invented_brand` 检查拒绝。
5. 画面中的恒温杯显示已稳定为 `98°F`；未将 `98°C` 误判为合格。
6. 预算采用强制模式；下一次预计费用超过剩余预算时，引擎会阻断真实调用，而不是仅记录告警。

## 未放行原因

第二镜仍出现非产品主体上的虚构文字。根据最终 QA 的“无虚构品牌/文字”要求，该镜头不能选用，因此没有批准 `take_gate`，也没有生成一份被错误标记为合格的 30 秒成片。

这说明真实适配器已经可调用且结构契约成立，但生成模型的视觉一致性仍需通过候选 Take、参考图和人工质检管理，不能以接口成功代替内容验收。

## 后续复验

1. 为第二镜补充“奶瓶无文字、无 logo、无刻度外品牌字符”的局部负向约束。
2. 在预算允许时只重生第二镜，不重复生成已通过的四镜。
3. 第二镜通过后选定五镜，批准 `take_gate`，合成 720 x 1280 / 30 秒成片。
4. 对成片抽帧并完成五项人工终审，再允许归档和交付。

> 本报告不包含真实密钥、带签名的供应商下载地址或生成媒体。运行媒体与临时数据库仅保存在 `data/reports/`，该目录不提交 Git。
